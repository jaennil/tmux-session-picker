[package]
name = "tmux-session-picker"
version = "0.1.0"
edition = "2024"

[dependencies]
libc = "0.2"
serde = { version = "1", features = ["derive"] }
toml = "0.9"

[profile.release]
codegen-units = 1
lto = "thin"
panic = "abort"
